# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']
    App\Service\CloudinaryService:
        arguments:
            $cloudName: '%env(string:CLOUDINARY_CLOUD_NAME)%'
            $apiKey: '%env(string:CLOUDINARY_API_KEY)%'
            $apiSecret: '%env(string:CLOUDINARY_API_SECRET)%'
    App\Service\Analytics\NvidiaAIClient:
        arguments:
            $apiKeyNvidia: '%env(NVIDIA_API_KEY)%'

    App\Service\BookingComService:
        arguments:
            $apiKey:  '%env(string:RAPIDAPI_KEY)%'
            $apiHost: '%env(string:RAPIDAPI_BOOKING_HOST)%'
    App\Service\AiChatService:
        arguments:
            $aiClient: '@App\Service\Analytics\NvidiaAIClient'
            $voyageService: '@App\Service\VoyageService'
            $offerService: '@App\Service\OfferService'

    App\Controller\AiChatController:
        tags: ['controller.service_arguments']

    App\Service\TwilioService:
        arguments:
            $accountSid: '%env(TWILIO_ACCOUNT_SID)%'
            $authToken: '%env(TWILIO_AUTH_TOKEN)%'
            $messagingServiceSid: '%env(TWILIO_MESSAGING_SERVICE_SID)%'

    App\Service\TranslationService:
        arguments:
            $apiKey: '%env(GOOGLE_TRANSLATE_API_KEY)%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
